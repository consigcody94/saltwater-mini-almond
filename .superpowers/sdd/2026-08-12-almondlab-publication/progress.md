# SDD ledger — plan: C:/Users/fowlb/Documents/Codex/2026-08-12/lets/docs/superpowers/plans/2026-08-12-almondlab-publication.md

Task 1: final narrow provenance repair ready for fresh independent rereview (POSIX cleanup never pathname-deletes after quarantine-handle verification and deliberately retains recoverable quarantine names; prepublication retention is `AtomicCleanupRetainedError(committed=False)`, while every post-publication retention/identity ambiguity is `AtomicCommitUncertainError(committed=True)` with exact recovery path(s); `run_manifest.json` is reserved case-insensitively as any artifact component across model, one-shot snapshot/finalize, semantic validator, and artifact-only schema; 169 focused tests passed with 3 real-POSIX integration cases platform-skipped on Windows, while deterministic post-`fstat` file/directory replacement schedules executed; combined concurrent tree was 938 passed, 25 registry-only failures, 3 skips, so stable full rerun remains required after registry repair)
