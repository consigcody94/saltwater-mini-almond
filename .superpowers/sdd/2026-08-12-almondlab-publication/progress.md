# SDD ledger — plan: C:/Users/fowlb/Documents/Codex/2026-08-12/lets/docs/superpowers/plans/2026-08-12-almondlab-publication.md

Task 1: identity-bound POSIX publication repair is ready to commit and then receive fresh exact rereview (file and run-directory publication retain the staged descriptor, prove the descriptor-relative `O_NOFOLLOW` name is the same regular-file/directory identity immediately before native no-replace rename, reconcile both names after every native return or exception, retain the verified target handle through post-validation and directory `fsync`, require the staged name to be absent, and expose a committed identity only after a final target proof; deterministic forced-POSIX schedules cover same-byte new-inode/directory/symlink swaps, the precheck-to-rename race, success, non-consuming success, exceptions before/after rename, collision, missing names, target substitution, replace mode, fsync disappearance, no hardlink use, and unavailable secure primitives; prepublication failures are `committed=False`, postpublication/ambiguous states are `committed=True`, unrelated concurrent targets are preserved, and recovery paths are handle-observed; focused provenance is 204 passed with 3 native-POSIX-only cases platform-skipped on Windows, and the final combined committed-baseline worktree is 1063 passed with 3 skips; native-POSIX execution and fresh independent exact rereview remain required)

Task 2: Round 2/5 registry and protected-join repair is committed at `9a90dfb` and proportionally verified, with fresh independent exact rereview still required (new RED command produced 10 expected failures and one inclusive-boundary control pass; `safe_join` now enforces an inclusive 10,000-row limit on each input, rejects exactly predicted oversized output before merge, and always checks the materialized result; C2 targeting is prospective and exact construct targeting/event mapping remain unresolved with build blocked; KaNa+/H+ separates paper-reported unversioned `MT473962` and its full-length coding-sequence claim from repository-resolved partial-CDS `MT473962.1` as an explicit identity/completeness conflict; rice 12/11/9 totals are recovered-line inventory rather than endpoint n, while germination remains 30 seeds/dish x 3 and other endpoint-specific independent-line n is `not_reported`; focused registry/safe suite is 120 passed, committed-baseline integration is 181 passed, relevant integration is 272 passed, full suite is 1057 passed with 3 documented Windows POSIX skips, and Phase 2 offline smoke is 22 passed, 0 failed)
