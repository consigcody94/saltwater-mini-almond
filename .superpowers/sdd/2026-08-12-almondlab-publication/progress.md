# SDD ledger — plan: C:/Users/fowlb/Documents/Codex/2026-08-12/lets/docs/superpowers/plans/2026-08-12-almondlab-publication.md

Task 1: final narrow provenance repair ready for fresh independent rereview (normal POSIX exclusive publication now consumes its temporary through secure atomic no-replace rename rather than hardlink-plus-cleanup; deterministic syscall schedules prove `atomic_create_bytes` and `finalize_manifest` return successfully with two validation phases, no hardlink, no retained temporary, and unavailable native rename fails prepublication with `committed=False`; POSIX rollback/error cleanup never pathname-deletes after quarantine-handle verification and deliberately retains recoverable quarantine names; post-publication retention/identity ambiguity is `AtomicCommitUncertainError(committed=True)` with exact recovery path(s); `run_manifest.json` remains reserved case-insensitively across all artifact boundaries; 172 focused tests passed with 3 real-POSIX integration cases platform-skipped on Windows, while deterministic post-`fstat` replacement schedules executed; a fresh combined-tree run and independent exact rereview remain required)
